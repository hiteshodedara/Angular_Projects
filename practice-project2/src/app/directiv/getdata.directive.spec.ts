import { GetdataDirective } from './getdata.directive';

describe('GetdataDirective', () => {
  it('should create an instance', () => {
    const directive = new GetdataDirective();
    expect(directive).toBeTruthy();
  });
});
